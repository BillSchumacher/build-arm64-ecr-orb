version: 2.1

description: >
  Builds an arm64 docker image, and then pushes the image to aws ecr. 
  Requires the aws orbs, probably not ecs but we'll include it for now.
display:
  home_url: "https://github.com/BillSchumacher/build-arm64-ecr-orb"
  source_url: "https://github.com/BillSchumacher/build-arm64-ecr-orb"

  orbs:
    aws-ecr: circleci/aws-ecr@8.1.2
    aws-ecs: circleci/aws-ecs@2.2.1
